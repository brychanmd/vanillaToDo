export default class ListView {
  /**
   *
   * @param {string} date
   */
  filterTasks(date) {
    switch (date) {
      case 'today':
        // do something
        break;

      case 'week':
        // do something
        break;

      default:
        break;
    }
  }
}
